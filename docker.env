# New-API Docker 构建环境变量配置
# 复制此文件为 docker.env 并修改相应配置

# 阿里云镜像仓库配置
DOCKER_REGISTRY=crpi-appxm8pdgvw49jw2.cn-hangzhou.personal.cr.aliyuncs.com/blueming3

# 默认版本标签 (如果不指定版本号，将使用此值)
TAG=latest
TAG=v1.0.0
# TAG=$(date +%Y%m%d%H%M%S)  # 使用时间戳

# Docker Hub 或其他仓库示例
# DOCKER_REGISTRY=your-dockerhub-username
# DOCKER_REGISTRY=your-private-registry.com/namespace

# 本地仓库示例
# DOCKER_REGISTRY=localhost:5000